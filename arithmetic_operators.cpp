#include <iostream>
#include <cmath>

int main(){

    // int students = 20;

    // students += 1;   //add by how many
    // students -= 2;
    // students *= 4;
    // students /= 2; 

    // std::cout << students;


    // int hellmen = 7;

    // hellmen = hellmen % 3; //mod has no shorthand

    // std::cout << hellmen;

    // double x = 3.14;
    // double y = 4;
    // double z;

    // z = std::max(x,y);
    // std::cout << z;

    // z = pow(2,3);
    // z = sqrt(9);
    // z =  abs(-3);
    // z = round(x);
    // z = ceil(x);
    // z = floor(x);
    // std::cout << z;

    return 0;
}